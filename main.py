from easy.two_sum import two_sum, arr
from easy.valid_subsequence import validate_subsequence, arr2, arr1
from easy.sorted_square_array import sort_square, arr

print(two_sum(arr, 10))
print(validate_subsequence(arr1, arr2))
print(sort_square(arr))
